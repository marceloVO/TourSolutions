<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}TourSolutions{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css')}}">
            <link rel="stylesheet" href="{{ asset('css/stylesheet.css')}}">
            
        {% endblock %}
    </head>
    <body>

        {% block body %}
            <nav class="navbar navbar-expand-lg ">
            <a class="navbar-brand" href="#"><img src="{{ asset('img/TourSolutions.png')}}" width="100" height="100"  class="logo-brand" alt="logo"></a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse " id="navbarText">
                <ul class="navbar-nav mr-auto ">
                <li class="nav-item active">
                    <a class="nav-link" href=" {{ path('dashboard') }} ">Home <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('RegistrarGaleria') }}">Subir Foto</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('MiGaleria') }}">Mi Galeria</a>
                </li>                
                
                </ul>
                <ul class="navbar-nav ">
                {% if app.user %}
                <li class="nav-item aling-items-right">
                    <a class="nav-link" href="{{ path('MiGaleria') }}">User: {{ app.user.username }}</a>
                </li>
                 <li class="nav-item aling-items-right">
                    <a class="btn btn btn-danger" href="{{ path('app_logout') }}">Logout</a>
                </li>               
                
                {% endif %}
                </ul>
            </div>
            </nav>
        
        {% endblock %}
        {% block javascripts %}
            <script src="{{ asset('js/jquery.min.js')}}"></script>
            <script src="{{ asset('js/bootstrap.min.js')}}"></script>
            
        {% endblock %}
    </body>
</html>
